## 目标

将 `frontend/src/pages/profile/index.vue` 中「web 服务卡（ServerCard）」与「profile 选择器 + 搜索输入框（page-header）」的上下位置对调：web 服务卡置顶，其下方为 profile 选择器与输入框，再往下依次是工具栏与插件列表。

## 修改内容（仅 1 个文件）

`frontend/src/pages/profile/index.vue`：

1. 把 `<ServerCard v-if="store.hasWebApp" />` 从 `page-body` 内移到模板最顶部（`<header class="page-header">` 之前），并用一个 `server-wrap` 容器包裹，给予 `padding: 16px 16px 0; flex-shrink: 0`，弥补它原先依赖 `page-body` 的 `padding: 16px` 与 `gap: 12px` 带来的间距。
2. `page-header`（profile 选择器 + 搜索框 + 设置按钮）原样保留，紧随服务卡之后。
3. `page-body` 中删除 ServerCard 相关代码，其余（工具栏、骨架屏、插件分组列表）保持不变。

## 调整后的页面顺序

1. web 服务卡（ServerCard，固定在页面顶部）
2. profile 选择器 + 搜索输入框 + 设置按钮（原 header）
3. 工具栏（安装 / 检查更新 / 导出 / 导入 / 纯净模式）
4. 插件列表（官方 / 三方分组）

## 说明

- 纯布局 / 样式微调，不涉及逻辑与数据结构，按 AGENTS.md 约定豁免文档更新。
- 不改动 ServerCard.vue、store 与任何脚本逻辑；文件总行数仍在 300 行红线内。