## 目标

移除首页（`pages/profile/index.vue`）插件列表的 HTML5 拖拽排序功能（用户判定无使用价值），删除整条拖拽链路及孤儿代码，同步更新文档。

## 改动清单

### 1. `frontend/src/pages/profile/components/PluginGroup.vue`
- 行 div 上移除：`draggable="true"`、`@dragstart`、`@dragover.prevent`、`@drop.prevent`、`@dragend`、`:class="{ dragging: ... }"`
- 移除 script 中：`dragIndex` ref、`onDragStart`、`onDrop` 函数
- `defineEmits` 移除 `move` 事件声明
- 模板 `v-for` 简化为 `v-for="item in items"`（不再需要 index）
- 样式移除 `.dragging` 块；保留 `.group-row` 的行 hover 高亮（通用列表交互，非拖拽专属）

### 2. `frontend/src/pages/profile/index.vue`
- 移除两处 `@move` 监听（official / third 的 PluginGroup）
- 删除 `// ---- 拖拽排序 ----` 注释及 `onMove` 函数（约 291–302 行）

### 3. `frontend/src/pages/profile/components/PluginCard.vue`
- 移除 `<drag-move-icon class="drag-handle" />`
- 移除 `DragMoveIcon` 图标导入
- 移除 `.drag-handle` 样式块

### 4. `frontend/src/store/dsh/index.ts`
- 删除 `moveBundle` 方法（仅被 `onMove` 调用，删除后无引用，属本次改动产生的孤儿代码）
- 同步修正上方注释 `// ---- 启用 / 禁用 / 排序 ----` → `// ---- 启用 / 禁用 ----`

### 5. 文档同步（RL-06）
- `docs/01-plugin-manager.md`：
  - 第 3 行描述：去掉"拖拽排序、"
  - 第 54 行 `pages/profile/index.vue` 一行：去掉"、拖拽排序"
  - 第 110 行注意事项 7：`不会打乱拖拽排序` → 改为追加序描述（`dsh plugin add` 只追加新包，顺序保持追加序）
- `docs/README.md` 索引描述：去掉"排序 /"

## 验证

- 运行 `pnpm check`（vue-tsc typecheck，RL-07），确认无类型错误
- 不做 build

## 不改动

- 不引入排序替代方案（如上下移按钮）——按用户意图仅移除
- `moveBundle` 依赖的 `dshApi.readProfileManifest / writeProfileManifest` 仍被其他逻辑使用，保留